plugins {
    id("snaproll.android.feature")
    id("snaproll.android.compose")
}

android {
    namespace = "com.ljh.snaproll.feature"
}

dependencies {
    // Basic jetpack libraries are already added by the feature plugin
}
